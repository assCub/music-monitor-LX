# 更新日志

本项目版本号遵循 [语义化版本 2.0.0](https://semver.org/lang/zh-CN/)：

- **major**（1.x.x → 2.0.0）：不兼容的变更，例如配置格式、卷挂载路径改了
- **minor**（1.0.x → 1.1.0）：新增功能，向后兼容
- **patch**（1.0.0 → 1.0.1）：修 bug，向后兼容

版本号的**唯一权威来源**是 `monitor/app/__init__.py` 里的 `__version__`。
请统一用脚本修改，不要手改其它文件：

```bash
./scripts/version.sh bump patch    # 递增并自动同步 Dockerfile / .env / CHANGELOG + 打 git tag
./scripts/version.sh check         # 校验各处版本号是否一致
```

镜像 tag 由 `docker-compose.yml` 从 `.env` 的 `APP_VERSION` 派生，统一发布到
`yua0712/music-monitor-lx`，同时维护各服务的最新标签。

## [Unreleased]

## [1.2.5] - 2026-09-22

## [1.2.4] - 2026-09-21

### 修复

- 根据现场扫码结果与上游能力声明，移除尚未调通的汽水扫码入口。汽水依赖动态风控签名，实际会持续返回限流状态而无法下发 Cookie 或验证码；现统一改为“其他平台”Cookie JSON 配置，成功后自动追加汽水标签。

## [1.2.3] - 2026-09-21

### 修复

- 修复汽水音乐扫码确认后一直显示“待手机确认”的问题：识别平台短信二次验证，支持发送并填写验证码或按提示完成上行短信验证。
- 扫码状态不再用固定文字覆盖平台返回结果；网易云、QQ、微信 QQ、酷狗、哔哩哔哩和汽水均显示统一、可读的中文状态。
- 真实扫码 token、汽水 `encrypt_uid` 和验证参数全部保留在服务端，浏览器只接收展示所需的脱敏信息。

## [1.2.2] - 2026-09-16

### 修复

- **输错一次密码，会把已经登录的会话整个弄丢**。之前只要登录接口失败（比如手滑打错一个字符），
  就无条件清掉当前会话 —— 哪怕那个会话还好好的。实测现场：用户 23:45 登录成功，
  之后 10 分钟会话一直有效（日志里受保护接口全是 200），23:56 又点了一次登录但密码打错了，
  会话当场被清空，看上去就成了「刷新一下就要重新登录」。
  现在失败时会先确认现有会话是否仍有效，有效就保留并明确提示「当前会话仍然有效，已保留」。

### 新增

- **会话自动恢复（真正的「固化」）**：只要设置里存过正确的账号密码，
  会话中途失效（引擎重启、会话过期、本服务重启后引擎当时没就绪）都会**自动补登录**，
  不用再手动登录一遍。打开页面时也会顺手检查并恢复一次。
  引擎的「代写 Cookie / 改引擎设置」在会话丢失时同样会先自动恢复再执行。
- 「引擎登录」一栏会显示已保存的账号（例如「已登录，会话有效（账号 admin）」），
  刷新页面就能看出来到底有没有记住，不用靠猜。

## [1.2.1] - 2026-09-15

### 修复

- **每次请求 `/favicon.ico` 服务端都在抛异常**：`204` 状态按 HTTP 规范不能带 body，
  之前返回的是 `JSONResponse({}, 204)`（带 2 字节 `{}`），uvicorn 每次都在发送阶段抛
  `RuntimeError: Response content longer than Content-Length`，并**打断那条 keep-alive 连接**。
  浏览器虽然仍收到合法的 204，但和它复用同一条连接的后续请求会一起断掉，表现像「点了没反应」。
  现在改为返回不带 body 的 `204`。

## [1.2.0] - 2026-09-15

### 修复

- **引擎登录「怎么输都成功」**：上游登录失败时返回的是 `200 + 登录页 HTML`（不带会话 Cookie），
  而之前只要 httpx 客户端的 Cookie 罐非空就算成功 —— 上一次成功登录留下的旧会话会让**任何乱输的账号密码都显示「登录成功」**，
  还会把原本正确的凭据覆盖掉，表现为「账号密码固化不下来」。
  现在改成：必须拿到 `music_dl_session` 且能通过受保护接口的二次校验才算登录成功；失败会明确报错并清掉旧会话。
- **已下载的歌每轮被重复推送**：引擎在「库里已有该歌」时返回 `skipped: true + path: ""`，
  只剩一个**没有扩展名**的 filename；旧代码直接把它当路径存下，下一轮就判定「文件不存在」→ 重新推送，
  引擎侧于是堆出一大堆 `skipped` 记录（实测攒到 266 条），两边数量永远对不上。
  现在会把裸 filename 还原成下载目录里的真实文件，并按「歌名+歌手」指纹认出换源过的曲目。
- **追踪任务刚结束就点「执行」没反应**：调度器只清理已结束任务的句柄，最长要等一个调度心跳
  （默认 60 秒）才能重新触发同一监控。
- 下载请求改用独立的长超时（`DOWNLOAD_TIMEOUT`，默认 300 秒）。
  无损单曲 30~80MB，上游要写完盘才回包，原来 30 秒默认超时会误判失败后重推，让引擎把同一条下载记成两次。
- 引擎管理员密码不再通过 `/api/settings` 回传给浏览器。

### 新增

- **分批推送下载**：一轮任务不再把整份清单一次性灌给引擎，而是「推一批 → 等这批全部落盘 → 再推下一批」。
  批次大小与间隔可配（`DOWNLOAD_BATCH` 默认 8、`DOWNLOAD_BATCH_GAP` 默认 3 秒），
  日志里会写清本轮上限、分几批、每批多少首。
- 运行记录新增「引擎已有」列与字段（`runs.engine_skipped`），
  把「本轮处理数」和「引擎侧实际落盘数」的口径差摆到明面上（老库自动补列，无需手工迁移）。
- 设置页新增「退出引擎登录」，用于丢弃失效会话与已保存的账号密码（以前没有出口，错误会话清不掉）。
- 「引擎连接」卡片会分别显示「是否登录」和「会话此刻是否有效」。

### 变更

- `MAX_DOWNLOADS_PER_RUN` 默认改为 `0`（不限），让每个监控自己的「单次最多下载」成为唯一上限 ——
  两个上限叠在一起时，日志里看不出到底被哪个卡住。需要硬保护时设成具体数字即可。

## [1.1.0] - 2026-09-15

### 新增

- 新增「歌手关注」监控类型：关注指定歌手，定期发现其新歌并自动下载
  - 上游没有「按歌手取全部歌曲」的接口，因此实现方式为「按歌手名搜索 + `exact_artist` 严格匹配 + 本地按歌手名二次过滤」，
    搜索结果里混入的翻唱、同名曲会被过滤掉（歌手字段含目标歌手即算，如「周杰伦/杨瑞代」这类合作曲）
  - 每行一个歌手，支持「歌手名|平台1,平台2」为单个歌手指定搜索平台；不写则使用监控级的平台列表
  - 与榜单/歌单/收藏夹完全共享后续的增量比对、音质择优、跨平台换源与下载去重

## [1.0.7] - 2026-09-14

### 修复

- QQ 音乐、酷狗音乐、酷我音乐的榜单不再「按链接未解析出曲目」：榜单页不是歌单页，上游无法解析，改为由本服务直接读取各平台公开榜单接口
- 榜单项改为记录平台榜单 ID，界面显示为「平台 榜单 ID」，并可在界面上「校验」

### 变更

- 内置榜单重新校准（QQ 14 个、酷狗 16 个、酷我 3 个、网易云 7 个），榜单 ID 均以平台官方榜单接口核实
- 移除 Apple Music 内置榜单：上游只能获取试听片段，而本服务会过滤试听片段，无法得到完整音频
- 移除 JOOX 内置榜单（其网页榜单页已下线），改由酷狗收录的「JOOX 香港热歌榜」覆盖

## [1.0.6] - 2026-09-12

### 修复

- 服务重启或任务取消后，运行记录不再永久停留在 `running`
- 启动时自动收尾上一次未完成的运行记录

## [1.0.5] - 2026-09-12

### 修复

- 删除已下载文件后，不再误判为“已在库中”，会重新进入下载流程
- 网页顶部显示当前 music-monitor 版本号
- 监控服务以只读方式检查下载文件是否真实存在

## [1.0.4] - 2026-09-12

## [1.0.3] - 2026-09-12

## [1.0.2] - 2026-09-12

## [1.0.1] - 2026-09-12

### 修复

- 下载前过滤试听片段、现场/演唱会/跨年等非正式版本
- 跨平台换源增加歌曲时长校验，避免下载错误版本

## [1.0.0] - 2026-09-12

首个正式版本。

### 新增

- 基于 go-music-dl 的「榜单 / 歌单 / 收藏夹」监控与自动下载服务
- 增量比对：只下载新出现的曲目，反复运行不会重复下载
- 音质择优：下载前探测真实码率，不达标自动到其它平台找更接近的版本（相似度 + 时长 + 可播放校验）
- 定时调度器，心跳间隔与下载并发可配置
- 零构建 Web 控制台：监控项管理、曲目记录、运行历史、手动触发、实时日志
- 引擎设置与登录态代写（扫码登录的 Cookie 决定能获取到的音质）

### 部署

- 提供 `docker-compose.yml`，一条命令拉起「引擎 + 监控」两个服务
- 镜像支持 `linux/amd64` 与 `linux/arm64`
- 配置与数据分离：配置集中在 `config/`（备份只需打包它），音乐文件放 `data/`
- 提供 `scripts/deploy-nas.sh`，在 NAS / Linux 上幂等部署（不在本地构建）

[Unreleased]: https://github.com/Baey666/music-monitor/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/Baey666/music-monitor/releases/tag/v1.0.0
